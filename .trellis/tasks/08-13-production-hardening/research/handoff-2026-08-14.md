# 交接档：2026-08-14 暂停现场（下次会话从这里直接继续）

> 暂停时 HEAD：`064967d`（分支 `codex/frontend-ink-prototype`，已含 Copilot 两提交：Trivy SHA 钉扎 + Gradle 产物清理）。工作区**有未提交改动**（快照见 §2），这是刻意的暂停状态，不是遗漏。

## 1. 恢复时的第一批动作（按序）

1. `cd backend && mvn -s .mvn/settings.xml verify`——**必须先跑**：检查代理 B 被中途停止，backend 可能处于半完成修复状态（见 §3），先确认可编译可过测。
2. 审 `git diff backend/src/main/java/com/example/codereview/agent/api/AgentRunService.java`——这是检查 B 的进行中改动（run 守卫「复制改复用」抽取），不在实施代理申报清单里。若改动完整（ReviewFeedbackService 已改为调用它）则保留；若半途则补完或回退到复制版（复制版本身可用）。
3. 补检查 B 的最后遗言指出的缺口：`ObjectLevelAuthorizationMatrixTest` 的 `expectDenied` 目前 400 也算过——要加固为「必须 authz 拒绝而非校验拒绝」。
4. 三个探针内联复核（不再派检查代理，用户已定极简检查规则）：CSRF 豁免是否精确限定 `/api/client-errors`；MISS_REPORT 幂等键（run+reporter+path+line）有无数据库级唯一索引强制（V28 的部分唯一索引只盖 FINDING_*，并发竞态待确认）；导出端点 ADMIN 判定同源性。
5. 全绿后走 §5 管线。

## 2. 暂停时工作区快照（git status --porcelain 排序）

**A 组（deploy/docs，检查 A 已过+5 处修复已入树）**：改 `deploy/nginx.conf`（仅 CSP 收紧）、`deploy/docker-compose.yml`、`deploy/observability/prometheus.yml`、`deploy/.env.example`、`docs/README.md`；新增 `deploy/tls/`（5 件）、`deploy/observability/`（alerts.yml/alertmanager.yml/alertmanager-entrypoint.sh/rabbitmq-enabled-plugins/README/grafana/）、`docs/13_数据生命周期与备份恢复.md`。
**B 组（backend，实施完成 mvn 657/0，检查中断）**：改 `RateLimitFilter.java`、`SecurityConfig.java`、`app-boundary.yml`、三个扩展测试类；新增 `finding/ReviewFeedback*` 六件、`clienterror/` 两目录、`V28__review_feedback.sql`、两个新测试类。
**检查 B 遗留改动**：`AgentRunService.java`（见 §1.2）。
**任务档**：`implement.md`（步骤 0 已勾）、`research/`（含本文件、server-acceptance-checklist.md、impl-notes-feedback-clienterr.md、verify-alertmanager-entrypoint-local.sh）。

## 3. 检查情况

- **检查 A（TLS/告警/docs）：已完成，结论通过**。发现并修复 5 处（全在树上）：DLQ 告警/看板/docs13 三处漏 `sandbox.dead.queue`（该队列由 sandbox-runner `SandboxRabbitConfig.java:20,38` 声明，backend RabbitMqConfig 只配路由不建队列——步骤 0 只核了 backend 侧所以漏）；docs/13 的 `deleteByTaskId` 归因错误（实为 `ReviewService.deleteTask:185`）；验收清单「8 条队列」实为 9。residual：implement.md 步骤 0 附记可补一句 sandbox.dead.queue（未做，不阻塞）。
- **检查 B（backend 反馈闭环/错误上报）：中途被停**（用户暂停指令）。已知它在做两件事：①`AgentRunService` run 守卫抽取复用（改动已在树，完整性未知）；②矩阵测试 `expectDenied` 加固（**未做**，它的中断遗言原文：「currently a 400 would satisfy expectDenied」）。其七维检查的其余结论**没有产出报告**——按极简检查规则不重派，按 §1.4 三探针内联补验即可。
- 实施代理 B 自验：`mvn verify` 657/0（Tests run: 657, Failures 0, Errors 0, Skipped 6=既有 Testcontainers 门控）；V28 只在有 Docker 的 CI/服务器真跑。

## 4. CI 问题诊断（已完成，修复方案已定未实施）

- 失败 run `31778306050`（PR #4）：`verify`（后端+前端+sandbox 全部测试）**绿**、`nginx-headers` **绿**；唯一挂的是 `supply-chain` 的「Scan images」步骤。
- 根因：`sandbox-runner` 镜像内 `/usr/local/bin/docker`（gobinary，来自基础镜像 `docker:29.6.2-cli`）含两个已有修复的 HIGH：**CVE-2026-39821**（stdlib/x-net idna punycode 提权）与 **CVE-2026-46600**（x/net dnsmessage DoS），修复版 Go 1.26.6/1.27.0-rc.3。其余三镜像 Total: 0。
- **修复方案**：`sandbox-runner/Dockerfile` 的 `FROM docker:29.6.2-cli AS docker-cli` → `docker:29.7.2-cli`（docker/cli 最新 tag，经 gh api 确认存在；暂停前正要查 release notes 确认其 Go 工具链 ≥1.26.6——可省略此确认，直接升级由 CI 验证，挂了再迭代）。本机无 Docker 无法预验。
- Copilot 的 Trivy SHA 钉扎（`1279b59`）针对的是另一件事（tag 漂移导致 action 拉取失败），保留无害；其误提交的 Gradle 产物已被 `064967d` 清理（已验证 `git ls-files` 无残留）。

## 5. 用户已批准的执行管线（恢复后按此走完）

① §1 的复核补漏 → ② `mvn verify` + `cd frontend && npm test && npm run build` 双终验 → ③ **一批提交**（A+B+检查修复+implement.md 勾账 1-5 阶段+CI 修复 Dockerfile bump，单笔，中文详述，本人署名无 AI 标记）→ ④ push 分支 → ⑤ `git checkout main && git merge --ff-only codex/frontend-ink-prototype && git push origin main` → ⑥ 清理（见 §6）→ ⑦ main 上派冗余清理+注释治理（backend 252 无中文文件；**不派检查代理**，内联终验，完成后一笔提交）→ ⑧ 提交时盯 CI（push main 触发），supply-chain 不绿就迭代镜像版本。尾段（frontend 错误上报接线+3 文件注释+sandbox 48 文件）等墨境步骤 7 落库后。

## 6. 分支清理清单（祖先关系已逐一验证）

- **纯祖先直接删**：本地+远程 `fix/defense-hardening`、`fix/track-a-core`、`fix/track-b-boundary`、`feat/pr-gatekeeper-agent`、远程 `integration/track-ab`。
- **含独有提交但已被取代，弃置**（向用户报告即可）：`feat/production-overhaul`（34 提交，六月大改旧线，功能已以演进形态在 HEAD：AgentPublication/sandbox 工具链等）；`codex/frontend-guofeng-cyber-redesign`（1 提交 `4826c5d` 8/12 旧版切片，被步骤 5/6 取代；本地是 worktree `.worktrees/frontend-guofeng-cyber-redesign`，先 `git worktree remove` 再删分支，远程同名分支一并删）；远程 `copilot/reference-29974411352`（2 提交实验）。
- worktree `.worktrees/pr-gatekeeper-agent`（b03ac2a，干净）同样 remove 后删分支。
- 全删后仅剩 `main`；PR #4（DRAFT，头分支即当前分支）随头分支删除自动关闭。当前分支本地删除前先 `git checkout main`。

## 7. 其他任务位置（不动，仅备忘）

- **墨境重构**：步骤 0/2/3/4/5/6 完成（最后提交 `0c43202`），下一步 7（动效整合）→8（逐页扩面+删旧表现层）→9（质量门，Node 22 正式验收+浏览器 QA）→10（收尾）。
- **r7**：只剩 38 例基线服务器复跑+收尾；**r8**：2a 代码已提交（`a9e7c3b`），2b 评测门禁等服务器；R3/R4 冻结等 2b 数字。
- **服务器转移清单**（用户自行安排环境）：r7 基线复跑、r8-R2 门禁、TLS/告警运行时验收（`research/server-acceptance-checklist.md`）。
- 工作偏好已入长期记忆：极简检查/合批提交/main 单分支。
