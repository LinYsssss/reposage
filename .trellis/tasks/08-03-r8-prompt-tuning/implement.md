# Implement：提示词调优

> **进度快照（2026-08-12）**：前置 r7 基线和 r4 prompt 规范已具备。R1 的代码实现已提交（`bdf07d4`：三层模板、版本化注册表、唯一组装入口、golden 测试）；由于该提交只做字节等价搬迁，内容性评测门禁仍需在后续改动中补齐。R2 已完成语料盘点和清单注入方案研究，但实现尚未开始；R3/R4、终版评测和漏报回灌演练均未完成。

0. [ ] 前置确认：r7 基线数字在档、r4 prompt 规范可被 hook 注入、真实模型配置可用（服务器）。
1. [ ] R1 分层模板：`resources/prompts/` 三层文件 + `PromptAssembler` 统一入口（评估扩展 `AgentPromptAssembler`）+ 组装单元测试（层缺失/超长截断）→ 评测对比 → 提交。
2a. [x] R2 代码实现：清单成文（Java 10 条/TS 3 条，SQL 迁移与 yml 配置弹药为零不建文件；每条附背书映射）+ 组装器按分片文件类型注入（方案 A：DiffSplitter.splitByFile 判定、java>ts 有序并集、空命中回落 generic）。（实况 2026-08-13：4 新模板 + 注册表挂载，既有 11 个 v1 模板零 diff 可回滚；13 条与研究稿逐字节相同（trellis-check 原始字节比对 + 突变探针实证 golden 非同义反复）；v2+generic==v1 字节等价延续 R1 保证；mvn verify 619/0；细节与分叉决策见 research/r2-impl-notes.md）
2b. [ ] R2 评测对比（服务器）：38 例语料、z-ai/glm-5.2 同参跑 v2 vs 基线两率 → 达标才允许合入 integration/对外引用。**已知最大回归风险：sec-java-* 六例晚于研究稿盘点，java 清单十条不含注入/越权/穿越条目，而 v2 替换了含此内容的六大类文本；若漏报率上升按规则三回炉，经步骤 6 回灌流程以 sec 用例为准入依据补条目。**复跑清单见 r2-impl-notes.md §5。
3. [ ] R3 两段式：复核段实现（同构 schema、只删不增的后端校验、失败降级保留初审）→ ai_call_log 两段可见 → 评测对比（重点看误报率下降、漏报率不动）→ 提交。
4. [ ] R4 动态 few-shot：选例 SQL + 注入预算裁剪 + 无数据降级 + 单元测试（两路径）→ 评测对比 → 提交。
5. [ ] 终版全量评测 vs r7 基线，数字落档 `eval-runs/final.md`；对外口径（README/简历素材）只引用此文件。
6. [ ] 漏报回灌流程文档化并演练一次（构造案例走全流程：发现→脱敏→入评测集→补清单条目）。
7. [ ] `trellis-check`（Agent）→ `trellis-update-spec` 沉淀（prompt 规范执行中的新教训）→ 提交推送 → `/trellis:finish-work`。

产物目录：`eval-runs/`（每步评测对比 + 终版对比）。
回滚点：R1-R4 各自独立提交；漏报率上升的改动直接不合入。
