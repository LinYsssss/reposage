# RepoSage 完整功能测试方案（服务器全量部署版）

> 适用前提：Docker、PostgreSQL+pgvector、RabbitMQ、Model Service、Sandbox Runner、Nginx 全部就绪
> 配套素材：`docs/演示素材与缺陷对照表.md`（两个演示仓库 + 25 条缺陷对照）
> 编写日期：2026-07-28

本文件覆盖系统**每一个功能模块**的验收用例。按模块编号（M1~M14），每条用例可独立执行、独立记录结果。

---

## 〇、执行前置

### 0.1 部署自检

| # | 检查项 | 命令 / 方式 | 通过标准 |
|---|---|---|---|
| 0-1 | 容器全部运行 | `docker compose ps` | 所有服务 `running`，健康检查 `healthy` |
| 0-2 | 后端健康 | `curl -s http://<host>/api/actuator/health` | `{"status":"UP"}` |
| 0-3 | 数据库连通 | health 响应含 db 组件 | `UP` |
| 0-4 | RabbitMQ 连通 | health 含 rabbit 组件 | `UP` |
| 0-5 | 模型服务 | `curl -s http://<host>/api/actuator/health` 查看 modelService | `UP` 或 `DEGRADED`（模型缺失时应为 DEGRADED，不能静默 UP） |
| 0-6 | **迁移已执行** | `select version, description, success from flyway_schema_history order by installed_rank` | 至少到 V21，全部 `success=t` |
| 0-7 | **只有 Nginx 对外暴露** | 从宿主外部访问 `<host>:8080`、`:5432`、`:5672`、`:8000` | **全部连不上**，只有 80/443 通 |
| 0-8 | 前端可访问 | 浏览器打开 `http://<host>` | 登录页正常渲染 |
| 0-9 | Actuator 收口 | 外部访问 `/actuator/prometheus` | 拒绝或 404（只允许容器内网抓取） |
| 0-10 | 调度器已启用 | 后端日志 | 无「跳过」告警；`app.agent.scheduling.enabled=true` |

> **0-6 与 0-7 是本轮最需要盯的两条**：迁移此前从未在真实数据库上执行过；端口收口是 Phase 6 的验收点。

### 0.2 准备账号

系统**没有注册接口**，首个账号由环境变量注入：

```
SEED_ADMIN_USERNAME=admin
SEED_ADMIN_PASSWORD=<至少 12 位强密码>
SEED_ADMIN_ROLE=ADMIN
```

测试越权需要**第二个普通账号**。若无管理界面创建用户，可临时用第二组 seed 变量启动一次，或直接在数据库插入（密码需 BCrypt）。

### 0.3 准备素材

- 演示仓库：`demo-repos/mall-order-service`、`demo-repos/payment-settlement-service`（各带 PR 分支）
- 知识文档：各仓库 `docs/` 下 4 个 `.md`
- 边界测试文件：见 M5.3

---

## M1 认证与会话

| # | 用例 | 步骤 | 预期 |
|---|---|---|---|
| 1-1 | 正常登录 | 用 seed 账号登录 | 200，进入主界面 |
| 1-2 | **Cookie 属性** | 开发者工具查看 `reposage_auth` | `HttpOnly` ✅、`Secure` ✅（HTTPS 下）、`SameSite=Lax/Strict` |
| 1-3 | **登录响应不含 Token** | 查看登录响应 JSON | **不含 `token` / `accessToken` 字段**（Track B 的 B2） |
| 1-4 | **前端不存 Token** | 查看 localStorage / sessionStorage | 无任何令牌 |
| 1-5 | 密码错误 | 输错密码 | 401，提示统一为「用户名或密码错误」 |
| 1-6 | 用户不存在 | 输入不存在的用户名 | 401，**提示与 1-5 完全一致**（不得区分） |
| 1-7 | **响应时间一致** | 对比 1-5 与 1-6 的响应耗时 | 差异不显著（dummy BCrypt 生效，防时序枚举） |
| 1-8 | **登录防爆破** | 同一 IP 连续错误登录 15 次 | 触发 429，响应含 `Retry-After` |
| 1-9 | 用户名维度限流 | 换 IP 但用同一用户名连错 6 次 | 同样触发 429 |
| 1-10 | 当前用户 | `GET /api/auth/me` | 返回 userId/username/nickname/role |
| 1-11 | 退出 | 点击退出 | Cookie 失效，跳回登录页 |
| 1-12 | 退出后访问 | 带旧 Cookie 访问 `/api/projects` | 401 |
| 1-13 | 无凭据访问 | 直接访问任意 `/api/...` | 401，不是 302 也不是 200 |
| 1-14 | **CSRF：无 Header 写请求** | 用 curl 带 Cookie 但不带 `X-XSRF-TOKEN` 发 POST | **403** |
| 1-15 | CSRF：正确 Header | 先 `GET /api/auth/csrf`，取 Cookie 值放入 Header | 成功 |
| 1-16 | **CSRF 登录后轮换** | 登录前后各取一次 Token | 两个值不同，旧值失效 |
| 1-17 | CSRF 退出后轮换 | 退出后重新获取 | 新值，旧值失效 |
| 1-18 | **生产密钥校验** | 用示例密钥（`dev-secret-change-me`）启动 prod | **启动失败**，日志明确指出哪个变量 |

---

## M2 项目管理

| # | 用例 | 预期 |
|---|---|---|
| 2-1 | 创建项目 | 200，返回 projectId |
| 2-2 | 项目列表 | 只返回**当前用户拥有的**项目 |
| 2-3 | 项目详情 | 正常返回 |
| 2-4 | 修改项目 | 名称/描述/默认分支更新成功 |
| 2-5 | 删除项目 | 成功，且关联数据被清理（见 2-8） |
| 2-6 | **越权：他人项目详情** | 账号 B 访问账号 A 的 projectId → **403**，`errorCode=PROJECT_FORBIDDEN` |
| 2-7 | **越权：他人项目删除** | 403 |
| 2-8 | 级联清理 | 删除项目后，其仓库/知识库/审查任务/报告均不可访问 |
| 2-9 | 不存在的项目 | 404，`errorCode=PROJECT_NOT_FOUND` |
| 2-10 | 超长名称 | 拒绝，400 |

---

## M3 仓库绑定与浏览

| # | 用例 | 预期 |
|---|---|---|
| 3-1 | 绑定 HTTPS 仓库 | 成功 |
| 3-2 | **凭据加密落库** | 查库 `code_repository.access_token_ciphertext` → `v1:` 前缀，**不含明文** |
| 3-3 | **响应不回显凭据** | 仓库详情响应**不含 accessToken** |
| 3-4 | 提交列表 | 返回提交记录，含 sha/作者/时间/message |
| 3-5 | 提交 diff | 正确显示变更文件与行 |
| 3-6 | **SSRF：私网地址** | 绑定 `https://192.168.1.1/x.git` → **拒绝** |
| 3-7 | **SSRF：回环** | `https://127.0.0.1/x.git` → 拒绝 |
| 3-8 | **SSRF：云元数据** | `https://169.254.169.254/x.git` → 拒绝 |
| 3-9 | **SSRF：IPv6 回环** | `https://[::1]/x.git` → 拒绝 |
| 3-10 | **SSRF：十进制 IP** | `https://2130706433/x.git`（=127.0.0.1）→ 拒绝 |
| 3-11 | **SSRF：userInfo** | `https://user:pass@evil.com/x.git` → 拒绝 |
| 3-12 | **SSRF：非白名单域名** | 绑定不在 `GIT_ALLOWED_HOSTS` 的域名 → 拒绝 |
| 3-13 | **本地路径在 prod 被拒** | provider=LOCAL → 拒绝（`app.git.allow-local-path=false`） |
| 3-14 | **Git 错误脱敏** | 绑定不存在的仓库 → 错误信息**不含本地路径、不含凭据、不含完整 URL query** |
| 3-15 | 超大仓库 | 克隆超出配置上限 → 明确失败，不吊死 |
| 3-16 | Git 超时 | 目标不可达 → 在配置超时内失败，**进程树被清理**（`docker exec` 查无残留 git 进程） |

---

## M4 Pull Request

| # | 用例 | 预期 |
|---|---|---|
| 4-1 | PR 列表 | 返回该项目下 PR |
| 4-2 | PR 详情 | 含标题、源/目标分支、head sha、状态 |
| 4-3 | PR 关联动作记录 | `actions` 返回审查动作历史 |
| 4-4 | **越权：他人项目 PR** | 403 |
| 4-5 | 不存在的 PR | 404，`errorCode=PULL_REQUEST_NOT_FOUND` |
| 4-6 | PR 跨项目校验 | 用 A 项目 ID + B 项目的 PR ID → 403/404，不得返回数据 |

---

## M5 知识库与 RAG

### 5.1 基础功能

| # | 用例 | 预期 |
|---|---|---|
| 5-1 | 上传 `.md` | 成功，状态最终为 `INDEXED` |
| 5-2 | 上传 `.txt` | 成功 |
| 5-3 | 文档列表 | **分页信封**：`{items,page,size,totalElements,totalPages}` |
| 5-4 | 语义检索 | 输入「订单发货规则」→ 返回 `order-flow.md` 相关片段，来源标注正确 |
| 5-5 | 检索 topK | 调整 topK，返回条数随之变化 |
| 5-6 | 重建索引 | `reindex` 成功，返回 indexed/skipped/failed 计数 |
| 5-7 | 删除文档 | 成功，其 chunk 与向量一并删除（查库确认） |
| 5-8 | **越权：他人项目知识库** | 403 |

### 5.2 事务与失败恢复（Track A 重点）

| # | 用例 | 步骤 | 预期 |
|---|---|---|---|
| 5-9 | **索引失败保留 FAILED** | 停掉 embedding 提供方（或配错 key），上传文档 | 接口报错，但**文档记录仍在**，状态 `FAILED`，`index_error` 有脱敏原因 |
| 5-10 | 失败后可重建 | 恢复 embedding 后执行 `reindex` | 该文档转为 `INDEXED` |
| 5-11 | **错误原因脱敏** | 查看 5-9 的 `index_error` | **不含提供方 URL、API Key、堆栈** |

### 5.3 输入边界（Track A 重点）

准备文件：

```powershell
$d="fixtures"; New-Item -ItemType Directory -Force $d | Out-Null
[IO.File]::WriteAllBytes("$d\fake.md",[byte[]](0x4D,0x5A,0x00,0x01,0x02,0x00))       # 二进制伪装
[IO.File]::WriteAllBytes("$d\broken.md",[byte[]](0xC3,0x28,0x61,0x62))                # 非法 UTF-8
[IO.File]::WriteAllText("$d\huge.md",("x"*3145728))                                   # 3MB
[IO.File]::WriteAllText("$d\payload.pdf","hello")                                     # 不支持扩展名
[IO.File]::WriteAllText("$d\blank.md","   `n`n  ")                                    # 空白
[IO.File]::WriteAllText("$d\good.md","# 标题`n`n正常中文正文内容。")                    # 对照组
```

| # | 上传 | 预期 |
|---|---|---|
| 5-12 | `good.md` | ✅ 接受，INDEXED |
| 5-13 | `fake.md` | ❌ 拒绝：「文档内容不是纯文本」 |
| 5-14 | `broken.md` | ❌ 拒绝：「不是有效的 UTF-8」 |
| 5-15 | `huge.md` | ❌ 拒绝：超出大小限制 |
| 5-16 | `payload.pdf` | ❌ 拒绝：仅支持 Markdown 和 TXT |
| 5-17 | `blank.md` | ❌ 拒绝：内容为空 |
| 5-18 | **路径穿越文件名** | 传 `..\..\..\etc\good.md` → 接受，但**存储与展示的文件名为 `good.md`** |
| 5-19 | **被拒绝不留残留** | 5-13~5-17 之后查文档列表 | **不产生任何 PENDING 记录** |
| 5-20 | 容器级上限 | 传 10MB 文件 | Nginx / multipart 层拦截，返回明确错误而非超时 |

---

## M6 传统代码审查

| # | 用例 | 预期 |
|---|---|---|
| 6-1 | 创建审查任务 | 成功，返回 taskId |
| 6-2 | 关联知识文档 | 创建时传 documentIds，审查结果体现文档上下文 |
| 6-3 | 任务列表 | **分页信封** |
| 6-4 | 任务详情 | 含状态、提交信息、耗时 |
| 6-5 | **幂等** | 对同一 (项目,仓库,commit,base,分支) 再次创建 | **返回已有任务**，不新建 |
| 6-6 | 取消任务 | 进行中的任务可取消 |
| 6-7 | 已结束任务取消 | 409，`errorCode=CONFLICT` |
| 6-8 | 删除任务 | 成功，关联日志一并清理 |
| 6-9 | 报告列表 | **分页信封** |
| 6-10 | 报告详情 | 含风险摘要、问题列表、证据、建议 |
| 6-11 | 导出 Markdown | 正常下载，`Content-Disposition` 文件名正确 |
| 6-12 | 导出 JSON | 正常下载 |
| 6-13 | **越权：他人报告** | 403 |
| 6-14 | 跨项目报告 | A 项目 ID + B 项目报告 ID → 403 |
| 6-15 | 空 diff | 提交无变更 → 明确提示，不产生空报告 |
| 6-16 | 超大 diff | 超过 `max-total-diff-chars` → 截断并在报告中说明 |

### 6.1 审查质量核验（对照 `演示素材与缺陷对照表.md`）

| # | 用例 | 预期 |
|---|---|---|
| 6-17 | 审查 `mall-order-service` PR 分支 | 命中对照表 M1~M10 中的多数 |
| 6-18 | 审查 `payment-settlement-service` PR 分支 | 命中 P1~P15 中的多数 |
| 6-19 | **RAG 生效验证** | 输出中引用文档名/章节，或指出与某条历史事故同类 |
| 6-20 | **对照实验** | 同一提交审查两次：一次关联知识文档、一次不关联 | 关联组应发现 B/C 类缺陷，未关联组基本只有 A 类 |
| 6-21 | **数值不一致检出** | 检查是否发现 P1（代码 0.8% vs 文档 0.6%） | 这条纯靠代码无法发现，是 RAG 的最强证据 |

---

## M7 反馈

| # | 用例 | 预期 |
|---|---|---|
| 7-1 | 提交反馈 | 成功 |
| 7-2 | 重复提交 | **更新而非新增** |
| 7-3 | 反馈列表 | 返回该问题下所有反馈 |
| 7-4 | 删除反馈 | 成功 |
| 7-5 | 删除不存在的反馈 | 404，`errorCode=FEEDBACK_NOT_FOUND` |
| 7-6 | 非法反馈类型 | 400 |
| 7-7 | **越权：他人问题的反馈** | 403（授权链 issue → report → task → project） |

---

## M8 Agent 运行（核心链路）

### 8.1 正常流程

| # | 用例 | 预期 |
|---|---|---|
| 8-1 | 创建 Agent Run | 成功，返回 runId，状态 `RECEIVED` |
| 8-2 | **自动推进** | 创建后**不做任何手工操作** | **1~2 秒内**自动进入下一 Step（P0-01 验证点） |
| 8-3 | Timeline | 展示各 Step 的状态、耗时、输入输出摘要 |
| 8-4 | **SSE 实时更新** | 打开 Timeline 页面观察 | Step 状态变化实时推送，无需刷新 |
| 8-5 | 状态机顺序 | 各 Step 按 `PREPARING_REPOSITORY → ANALYZING_CHANGE → PLANNING → ...` 推进 | 无跳跃 |
| 8-6 | Run 列表 | 按项目查询，返回运行历史 |
| 8-7 | 取消运行 | 进行中可取消，状态转 `CANCELED` |
| 8-8 | 已结束取消 | 409，`errorCode=AGENT_RUN_CONFLICT` |
| 8-9 | 重试运行 | 失败/超时的运行可重试 |
| 8-10 | 重试成功的运行 | 409（只有失败或超时可重试） |
| 8-11 | **越权：他人 Run** | 403 |
| 8-12 | 预算耗尽 | 超过 `max-elapsed` / `max-tool-calls` | 明确转 FAILED，原因清晰 |

### 8.2 Outbox 可靠性（Track A P0-01~03 验证）

| # | 用例 | 步骤 | 预期 |
|---|---|---|---|
| 8-13 | **Broker 不可用不产生假 SENT** | `docker compose stop rabbitmq`，创建 Run，查 `agent_outbox_event` | 事件**不出现 SENT**，停留 PENDING/PROCESSING 并累积 attempt |
| 8-14 | **Broker 恢复后自动续跑** | 启动 rabbitmq | 事件自动转 SENT，Run 继续推进，**无需人工干预** |
| 8-15 | **租约回收** | 手工把某事件置为 PROCESSING 且 `lease_expires_at` 设为过去时间 | reaper 在一个周期内将其转回 PENDING，`attempt_count` +1 |
| 8-16 | **终态 FAILED** | 构造一个永远不可路由的事件（改 event_type 为无效值） | 达到 `max-attempts` 后转 **FAILED**，`failed_at` 有值，不再重发 |
| 8-17 | 不可路由不标 SENT | 同上过程中观察 | 全程无 SENT |
| 8-18 | 多实例不重复认领 | 启两个后端实例 | 同一事件只被一个实例发布（查 `claim_token`） |

### 8.3 Step 执行租约（Track A P0-04 验证）

| # | 用例 | 步骤 | 预期 |
|---|---|---|---|
| 8-19 | **执行期间无长事务** | Run 进行中，在 PG 执行 `select * from pg_stat_activity where state='idle in transaction'` | **无长时间挂起的业务事务** |
| 8-20 | **连接池不被占满** | 并发发起 5 个 Run | 连接数平稳，不耗尽 |
| 8-21 | **Worker 崩溃可恢复** | Run 执行中 `docker compose kill backend`，重启 | Step 租约过期后被 watchdog 回收，Run 继续或明确 FAILED，**不永久卡 RUNNING** |
| 8-22 | **心跳续租** | 让某 Step 执行较久（大 diff） | 该 Step **不被 watchdog 误杀**（查 `lease_expires_at` 在推进） |
| 8-23 | **重复消息不重复执行** | 手工向 `agent.step.queue` 重投同一消息 | 返回忽略，**不产生重复副作用** |
| 8-24 | 周期恢复 | 进程不重启的情况下制造一个卡死 Step | 60 秒内被 watchdog 处理（P1-04） |

---

## M9 Finding 与 Patch

| # | 用例 | 预期 |
|---|---|---|
| 9-1 | Finding 列表 | 返回该 Run 的问题，含严重度、置信度、证据 |
| 9-2 | Finding 去重 | 同一问题不重复出现 |
| 9-3 | 置信度范围 | 所有 confidence 落在 0~1 |
| 9-4 | 被拒绝的 Finding | UI 上与正常 Finding 有区分 |
| 9-5 | Patch 候选列表 | 返回生成的补丁 |
| 9-6 | Patch diff 展示 | 正确渲染，**按纯文本渲染不执行 HTML** |
| 9-7 | **Sandbox 验证** | Patch 在沙箱中编译/测试 | 返回验证结果（build/test/scan 状态） |
| 9-8 | 审批通过 | 状态转 APPROVED |
| 9-9 | 审批拒绝 | 需填拒绝原因，状态转 REJECTED |
| 9-10 | **重复审批** | 对已审批的 Patch 再次审批 | 409，`errorCode=PATCH_APPROVAL_CONFLICT` |
| 9-11 | **过期 Patch** | head sha 变更后审批 | 409，`errorCode=PATCH_STALE` |
| 9-12 | 未通过校验的 Patch | 尝试批准 | 409，明确拒绝 |
| 9-13 | **越权审批** | 账号 B 审批账号 A 项目的 Patch | 403 |
| 9-14 | **路径穿越 Patch** | 构造含 `../` 的补丁 | 拒绝 |

---

## M10 Sandbox Runner

| # | 用例 | 预期 |
|---|---|---|
| 10-1 | 正常执行 | 白名单命令正常运行并返回结果 |
| 10-2 | **无网络** | 沙箱内 `curl example.com` | 失败（网络已禁用） |
| 10-3 | **非 root** | 容器内 `id` | 非 root 用户 |
| 10-4 | **只读根文件系统** | 尝试写 `/` | 失败 |
| 10-5 | CPU/内存限制 | 跑消耗型任务 | 被限制，不拖垮宿主 |
| 10-6 | PID 限制 | fork 炸弹 | 被限制 |
| 10-7 | 超时终止 | 死循环任务 | 到时被杀，进程树清理干净 |
| 10-8 | 输出上限 | 产生海量输出 | 被截断，不撑爆内存 |
| 10-9 | **签名校验** | 伪造任务消息 | 拒绝，`errorCode=SANDBOX_SIGNATURE_INVALID` |
| 10-10 | **重放拒绝** | 重复投递同一 nonce | 拒绝，`errorCode=SANDBOX_REPLAY_REJECTED` |
| 10-11 | 过期签名 | 超过 expiresAt 的任务 | 拒绝 |
| 10-12 | 非白名单命令 | 提交 `rm -rf /` | 拒绝 |
| 10-13 | symlink 逃逸 | 构造符号链接指向宿主路径 | 拒绝或无效 |
| 10-14 | 压缩炸弹 | 提交高压缩比文件 | 被大小限制拦截 |

---

## M11 SCM 集成与 Webhook

| # | 用例 | 预期 |
|---|---|---|
| 11-1 | 创建 SCM 安装配置 | 成功，凭据加密存储 |
| 11-2 | 配置列表 | **不回显 secret 明文** |
| 11-3 | 更新配置 | 成功 |
| 11-4 | 删除配置 | 成功 |
| 11-5 | **GitHub PR opened** | 从 GitHub 发起真实 PR | 收到 webhook，自动创建 Agent Run |
| 11-6 | GitHub synchronize | PR 推新提交 | 触发新的审查 |
| 11-7 | GitHub reopened | 重开 PR | 正常处理 |
| 11-8 | 无关事件 | 如 labeled | 忽略，不创建 Run |
| 11-9 | **错误签名** | 篡改 payload 或签名 | **401**，`errorCode=WEBHOOK_SIGNATURE_INVALID` |
| 11-10 | **未验签不落正文** | 发送错误签名请求后查 `scm_webhook_delivery` | **不保存请求正文**，不泄露 runId |
| 11-11 | **重复 delivery** | 同一 deliveryId 重复投递 | 幂等，返回既有结果，**不 500** |
| 11-12 | **并发相同 delivery** | 同时发 5 个相同 deliveryId | 只处理一次，无唯一键冲突报错 |
| 11-13 | 未知安装 | 伪造 installationRef | 404 或拒绝，不落正文 |
| 11-14 | 超长 deliveryId | 构造 10KB 的 header | 拒绝 |
| 11-15 | 超大 Body | 发送 50MB payload | Nginx 拦截 |
| 11-16 | **回调正文不落库** | 正常处理后查 `payload_preview` | 默认为空，只有 `payload_hash` 等元数据 |
| 11-17 | GitLab 全套 | 重复 11-5~11-16 的 GitLab 版本 | 同样通过 |
| 11-18 | **SCM 回写** | 审批通过的 Patch 发布回 PR | PR 上出现审查评论 |
| 11-19 | 回写失败处理 | 断开 SCM 网络后发布 | 明确失败，可重试，不假成功 |

---

## M12 日志、监控与可观测性

| # | 用例 | 预期 |
|---|---|---|
| 12-1 | MQ 日志 | **分页信封**，按 taskId 查询 |
| 12-2 | **MQ 日志越权** | 账号 B 查账号 A 的 taskId | **403**，`errorCode=PROJECT_FORBIDDEN`，响应体不含日志 |
| 12-3 | AI 调用日志 | 含 token 数、耗时、成本估算 |
| 12-4 | AI 日志越权 | 403 |
| 12-5 | **traceId 贯通** | 发一个请求，记下响应头 `X-Trace-Id` | 后端日志中能用该 id 检索到完整链路 |
| 12-6 | 响应体含 traceId | 任意错误响应 | 含 `traceId` 字段，与响应头一致 |
| 12-7 | Prometheus 指标 | 容器内 `curl backend:8080/actuator/prometheus` | 含 `reposage.agent.step`、`reposage.agent.tool` 等自定义指标 |
| 12-8 | **Prometheus 不对外** | 宿主外访问 | 拒绝 |
| 12-9 | OTel Trace | 查看 collector | Agent Step 的 span 可见 |
| 12-10 | 健康检查分级 | 停掉模型服务 | health 转 `DEGRADED` 而非静默 UP |

---

## M13 API 通用行为

| # | 用例 | 预期 |
|---|---|---|
| 13-1 | 分页默认值 | 不传参 | `size=20, page=0` |
| 13-2 | **分页上限** | `size=10000` | 返回 `size=100` |
| 13-3 | 非法分页 | `page=-5&size=-3` | 回落 `page=0,size=20`，**不报错** |
| 13-4 | 越界页 | `page=999` | `items` 空数组，`totalElements` 正确，200 |
| 13-5 | **错误码** | 各类错误响应 | 含 `errorCode` 字符串（如 `AGENT_RUN_CONFLICT`） |
| 13-6 | **状态码语义** | 逐项核对 | 400 参数错 / 401 未登录 / 403 越权 / 404 不存在 / 409 冲突 / 429 限流 / 503 依赖不可用 |
| 13-7 | **错误不泄露内部信息** | 制造各类错误 | **不含堆栈、SQL、本地路径、内部 URL、模型原始错误** |
| 13-8 | 限流 | 高频请求 | 429 + `Retry-After` |
| 13-9 | **代理头不可伪造** | 伪造 `X-Forwarded-For` 绕过限流 | 无效（Nginx 覆盖为 remote_addr） |
| 13-10 | 非法 JSON | 发送畸形请求体 | 400，提示清晰 |

---

## M14 前端行为

| # | 用例 | 预期 |
|---|---|---|
| 14-1 | 无硬编码账号 | 登录页用户名框 | **默认为空**，不预填演示账号 |
| 14-2 | 无本机路径 | 仓库绑定页 | 不出现 `F:\202605New` 之类的绝对路径 |
| 14-3 | 分页 UI | 各列表页 | 有分页控件，切页正常 |
| 14-4 | 401 单次失效 | 让 Cookie 过期后并发多个请求 | **只跳转一次**登录页，无跳转风暴 |
| 14-5 | 403 不登出 | 访问无权资源 | 提示「无权访问」，**保持登录态** |
| 14-6 | 409 刷新 | 冲突操作 | 提示冲突并刷新相关资源 |
| 14-7 | 429 提示 | 触发限流 | 读取 `Retry-After`，禁用按钮并倒计时 |
| 14-8 | 5xx 展示 traceId | 服务端错误 | 显示 traceId 供排查，**不显示堆栈** |
| 14-9 | **按钮防重复** | 快速连点提交按钮 | 只提交一次，busy 期间禁用 |
| 14-10 | 失败后恢复 | 请求失败 | 按钮恢复可用（finally） |
| 14-11 | 空/加载/失败态 | 各列表页 | 三种状态都有明确呈现 |
| 14-12 | **SSE 退出即关闭** | 退出登录 | SSE 连接立即 close，无残留请求 |
| 14-13 | SSE 断线退避 | 断网再恢复 | 指数退避重连，超阈值回退轮询 |
| 14-14 | **SSE 不带 query Token** | 查看 SSE 请求 URL | **不含令牌**，走同源 Cookie |
| 14-15 | **XSS：模型输出** | 让 AI 输出含 `<img src=x onerror=alert(1)>` | **按纯文本渲染**，不执行 |
| 14-16 | XSS：Diff | Diff 含 HTML 标签 | 纯文本渲染 |
| 14-17 | 外链安全 | 所有 `target="_blank"` | 带 `rel="noopener noreferrer"` |
| 14-18 | 分辨率适配 | 1366×768 与 1920×1080 | 核心操作无遮挡，焦点清晰 |

---

## M15 端到端链路

| # | 链路 | 步骤 | 预期 |
|---|---|---|---|
| 15-1 | **完整 PR 守门链路** | GitHub 开 PR → Webhook → Agent Run → Finding → Patch → 沙箱验证 → 人工审批 → 回写 PR | 全程自动流转，PR 上出现审查结果 |
| 15-2 | **传统审查链路** | 绑仓库 → 传文档 → 建审查 → 看报告 → 导出 | 全程通畅 |
| 15-3 | 连续演示 3 次 | 重复 15-1 三遍 | **三次均成功**，无需重启服务 |
| 15-4 | 混合负载 | 同时跑 3 个 Agent Run + 2 个传统审查 | 互不干扰，均完成 |

---

## M16 故障注入与恢复

| # | 场景 | 操作 | 预期 |
|---|---|---|---|
| 16-1 | **数据库重启** | `docker compose restart postgres` | 连接池恢复，进行中任务可恢复或明确失败 |
| 16-2 | **RabbitMQ 重启** | `docker compose restart rabbitmq` | 见 8-13/8-14，无假 SENT |
| 16-3 | **后端重启** | `docker compose restart backend` | 卡住的 Step 被恢复，Run 继续 |
| 16-4 | **模型服务不可用** | `docker compose stop model-service` | health 转 DEGRADED，规则降级仍可出结果，指标有记录 |
| 16-5 | **Sandbox Runner 不可用** | 停掉 runner | Patch 验证明确失败，不永久 pending |
| 16-6 | 缺少环境变量 | 删掉必需变量重启 | **快速失败**，日志指明缺哪个 |
| 16-7 | 磁盘写满 | 模拟磁盘满 | 明确报错，不静默丢数据 |

---

## M17 数据与运维

| # | 用例 | 预期 |
|---|---|---|
| 17-1 | **迁移全部成功** | 查 `flyway_schema_history` | 全部 `success=t`，无 pending |
| 17-2 | **备份** | `pg_dump` 导出 | 成功，记录耗时与体积 |
| 17-3 | **恢复演练** | 恢复到临时库并校验关键表行数 | 数据一致 |
| 17-4 | 约束生效 | 尝试插入孤儿数据 | 被外键拒绝（V22~V24 完成后） |
| 17-5 | 日志轮转 | 长时间运行 | 日志不无限增长 |
| 17-6 | 资源限制 | 查看 compose 配置 | 各服务有 CPU/内存上限 |

---

## M18 供应链与依赖

| # | 用例 | 命令 | 预期 |
|---|---|---|---|
| 18-1 | 后端测试 | `mvn -s .mvn/settings.xml verify` | 全绿，**3 个 Testcontainers 测试不再 skipped** |
| 18-2 | Runner 测试 | `mvn test` | 全绿 |
| 18-3 | 前端测试 | `npm test` | 全绿 |
| 18-4 | 前端构建 | `npm run build` | 成功 |
| 18-5 | npm 漏洞 | `npm audit --audit-level=high` | **0 个 high/critical** |
| 18-6 | Maven 漏洞 | 依赖扫描插件 | 0 个 high/critical，或有备案豁免 |
| 18-7 | Python 漏洞 | `pip-audit -r requirements.txt` | 同上 |
| 18-8 | 镜像扫描 | `trivy image <各镜像>` | 0 个 high/critical，或有备案豁免 |
| 18-9 | 镜像无残留 | 检查镜像内容 | 无包管理缓存、测试密钥、源码凭据 |
| 18-10 | JaCoCo | verify 产物 | line ≥ 70%，branch ≥ 50% |

---

## 附：结果记录模板

建议按下表逐条记录，**区分三档结论**：

| 编号 | 用例 | 结果 | 证据 | 备注 |
|---|---|---|---|---|
| 8-13 | Broker 不可用不产生假 SENT | ✅ 通过 | 截图 / SQL 输出 | |
| 8-21 | Worker 崩溃可恢复 | ⚠️ 部分 | | 恢复耗时 90s，超出预期 |
| 10-13 | symlink 逃逸 | ❌ 未验证 | | 缺少构造条件 |

三档定义：

- **✅ 通过** —— 实际执行且结果符合预期，有证据
- **⚠️ 部分 / 有条件通过** —— 功能可用但有偏差，需说明
- **❌ 未验证** —— 未执行或缺少条件。**不得写成通过**

---

## 附：优先级建议

用例共 200+ 条，若时间有限，按此顺序：

**第一优先（必测，答辩会问）**
M8 全部（Agent 链路 + Outbox + 租约）、M15-1（PR 守门全链路）、M6.1（审查质量与 RAG 验证）、M12-2（MQ 越权）、M1-14~18（CSRF 与密钥）

**第二优先（安全项，出问题最难解释）**
M3-6~16（SSRF 与 Git 边界）、M11-9~16（Webhook 验签与幂等）、M10（Sandbox 隔离）、M5.3（上传边界）

**第三优先（工程完备性）**
M16 故障注入、M17 备份恢复、M18 供应链

**第四优先（体验）**
M14 前端行为、M13 API 通用行为

---

## 附：本轮特别需要盯的三处

这三项此前**从未在真实环境验证过**，服务器部署后是第一次：

1. **Flyway 迁移链首次真实执行**（0-6、17-1）—— V19/V20/V21 此前只在 H2 从实体建表的环境下跑过，从未经过 Flyway。若失败，优先看 `alter table ... add column if not exists` 在目标 PG 版本的兼容性。
2. **三个 Testcontainers 测试首次运行**（18-1）—— 它们此前一直 skipped。
3. **端口收口**（0-7、12-8）—— 只有 Nginx 应对外暴露，数据库/MQ/后端管理端口都不应可达。
