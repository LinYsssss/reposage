diff --git a/src/main/java/com/example/settlement/controller/JsonSupport.java b/src/main/java/com/example/settlement/controller/JsonSupport.java
new file mode 100644
index 0000000..02eca6f
--- /dev/null
+++ b/src/main/java/com/example/settlement/controller/JsonSupport.java
@@ -0,0 +1,14 @@
+package com.example.settlement.controller;
+
+import java.util.Map;
+
+/** 极简 JSON 解析，演示仓库用。 */
+final class JsonSupport {
+
+    private JsonSupport() {
+    }
+
+    static Map<String, Object> parse(String raw) {
+        throw new UnsupportedOperationException("demo");
+    }
+}
diff --git a/src/main/java/com/example/settlement/controller/PayoutCallbackController.java b/src/main/java/com/example/settlement/controller/PayoutCallbackController.java
new file mode 100644
index 0000000..6d14d09
--- /dev/null
+++ b/src/main/java/com/example/settlement/controller/PayoutCallbackController.java
@@ -0,0 +1,43 @@
+package com.example.settlement.controller;
+
+import com.example.settlement.repository.PayoutCallbackLogRepository;
+import java.util.Map;
+
+/**
+ * 银行代付结果回调入口。
+ */
+public class PayoutCallbackController {
+
+    private final PayoutCallbackLogRepository callbackLogs;
+    private final SettlementStatusUpdater statusUpdater;
+
+    public PayoutCallbackController(PayoutCallbackLogRepository callbackLogs,
+                                    SettlementStatusUpdater statusUpdater) {
+        this.callbackLogs = callbackLogs;
+        this.statusUpdater = statusUpdater;
+    }
+
+    /**
+     * 接收银行回调。
+     *
+     * @param rawBody   原始请求体
+     * @param headers   请求头，含签名
+     */
+    public String onPayoutResult(String rawBody, Map<String, String> headers) {
+        Map<String, Object> event = JsonSupport.parse(rawBody);
+
+        String merchantNo = String.valueOf(event.get("merchantNo"));
+        String settlementId = String.valueOf(event.get("settlementId"));
+        String resultCode = String.valueOf(event.get("resultCode"));
+
+        callbackLogs.save(settlementId, merchantNo, rawBody, resultCode);
+
+        if ("SUCCESS".equals(resultCode)) {
+            statusUpdater.markSuccess(Long.valueOf(settlementId));
+        } else {
+            statusUpdater.markFailed(Long.valueOf(settlementId));
+        }
+
+        return "ok";
+    }
+}
diff --git a/src/main/java/com/example/settlement/controller/RefundController.java b/src/main/java/com/example/settlement/controller/RefundController.java
new file mode 100644
index 0000000..32fc32a
--- /dev/null
+++ b/src/main/java/com/example/settlement/controller/RefundController.java
@@ -0,0 +1,35 @@
+package com.example.settlement.controller;
+
+import com.example.settlement.service.RefundService;
+
+/**
+ * 商户退款入口。
+ */
+public class RefundController {
+
+    private final RefundService refundService;
+
+    public RefundController(RefundService refundService) {
+        this.refundService = refundService;
+    }
+
+    /**
+     * 发起退款。
+     *
+     * @param amountYuan 退款金额，单位元
+     */
+    public String refund(Long tenantId, Long merchantId, String orderNo, double amountYuan) {
+        long amountFen = (long) (amountYuan * 100);
+        refundService.refund(tenantId, merchantId, orderNo, amountFen);
+        return "accepted";
+    }
+
+    /**
+     * 管理员强制退款，用于处理客诉。
+     */
+    public String forceRefund(Long tenantId, Long merchantId, String orderNo, double amountYuan) {
+        long amountFen = (long) (amountYuan * 100);
+        refundService.forceRefund(tenantId, merchantId, orderNo, amountFen);
+        return "accepted";
+    }
+}
diff --git a/src/main/java/com/example/settlement/controller/SettlementStatusUpdater.java b/src/main/java/com/example/settlement/controller/SettlementStatusUpdater.java
new file mode 100644
index 0000000..2e6ec4d
--- /dev/null
+++ b/src/main/java/com/example/settlement/controller/SettlementStatusUpdater.java
@@ -0,0 +1,9 @@
+package com.example.settlement.controller;
+
+/** 结算状态更新。 */
+public interface SettlementStatusUpdater {
+
+    void markSuccess(Long settlementId);
+
+    void markFailed(Long settlementId);
+}
diff --git a/src/main/java/com/example/settlement/repository/MerchantQueryRepository.java b/src/main/java/com/example/settlement/repository/MerchantQueryRepository.java
new file mode 100644
index 0000000..623956b
--- /dev/null
+++ b/src/main/java/com/example/settlement/repository/MerchantQueryRepository.java
@@ -0,0 +1,40 @@
+package com.example.settlement.repository;
+
+import java.util.List;
+import java.util.Map;
+import javax.sql.DataSource;
+
+/**
+ * 运营后台商户查询。
+ */
+public class MerchantQueryRepository {
+
+    private final DataSource dataSource;
+
+    public MerchantQueryRepository(DataSource dataSource) {
+        this.dataSource = dataSource;
+    }
+
+    /**
+     * 按商户名模糊搜索，供运营后台使用。
+     */
+    public List<Map<String, Object>> searchByName(String keyword, String orderBy) {
+        String sql = "select merchant_id, merchant_name, risk_level, total_amount"
+                + " from merchant_summary"
+                + " where merchant_name like '%" + keyword + "%'"
+                + " order by " + orderBy;
+        return RawJdbc.queryList(dataSource, sql);
+    }
+
+    /**
+     * 导出指定商户的结算汇总。
+     */
+    public List<Map<String, Object>> exportSettlementSummary(Long merchantId, String from, String to) {
+        String sql = "select merchant_id, sum(net_amount) as total, count(*) as cnt"
+                + " from settlement_request"
+                + " where merchant_id = " + merchantId
+                + " and created_at between '" + from + "' and '" + to + "'"
+                + " group by merchant_id";
+        return RawJdbc.queryList(dataSource, sql);
+    }
+}
diff --git a/src/main/java/com/example/settlement/repository/PayoutCallbackLogRepository.java b/src/main/java/com/example/settlement/repository/PayoutCallbackLogRepository.java
new file mode 100644
index 0000000..cccdd7c
--- /dev/null
+++ b/src/main/java/com/example/settlement/repository/PayoutCallbackLogRepository.java
@@ -0,0 +1,7 @@
+package com.example.settlement.repository;
+
+/** 银行回调日志存储。 */
+public interface PayoutCallbackLogRepository {
+
+    void save(String settlementId, String merchantNo, String payload, String resultCode);
+}
diff --git a/src/main/java/com/example/settlement/repository/RawJdbc.java b/src/main/java/com/example/settlement/repository/RawJdbc.java
new file mode 100644
index 0000000..e7dac5b
--- /dev/null
+++ b/src/main/java/com/example/settlement/repository/RawJdbc.java
@@ -0,0 +1,16 @@
+package com.example.settlement.repository;
+
+import java.util.List;
+import java.util.Map;
+import javax.sql.DataSource;
+
+/** 直接执行 SQL 字符串，演示仓库用。 */
+final class RawJdbc {
+
+    private RawJdbc() {
+    }
+
+    static List<Map<String, Object>> queryList(DataSource dataSource, String sql) {
+        throw new UnsupportedOperationException("demo");
+    }
+}
diff --git a/src/main/java/com/example/settlement/service/InstantSettlementService.java b/src/main/java/com/example/settlement/service/InstantSettlementService.java
new file mode 100644
index 0000000..675f0f6
--- /dev/null
+++ b/src/main/java/com/example/settlement/service/InstantSettlementService.java
@@ -0,0 +1,49 @@
+package com.example.settlement.service;
+
+import com.example.settlement.model.SettlementRequest;
+import com.example.settlement.repository.SettlementRequestRepository;
+
+/**
+ * 即时结算（T+0），运营侧新需求。
+ *
+ * <p>为了尽快上线，先按标准商户费率处理，后续再对接配置表。
+ */
+public class InstantSettlementService {
+
+    /** 标准商户费率 0.8% */
+    private static final double FEE_RATE = 0.008;
+
+    private final SettlementRequestRepository repository;
+
+    public InstantSettlementService(SettlementRequestRepository repository) {
+        this.repository = repository;
+    }
+
+    /**
+     * 发起即时结算。
+     *
+     * @param grossAmountYuan 结算总额，单位元
+     */
+    public SettlementRequest submitInstant(Long tenantId, Long merchantId,
+                                           double grossAmountYuan, String currency) {
+        double fee = grossAmountYuan * FEE_RATE;
+        double net = grossAmountYuan - fee;
+
+        long grossFen = Math.round(grossAmountYuan * 100);
+        long feeFen = Math.round(fee * 100);
+
+        String key = "instant-" + merchantId + "-" + System.currentTimeMillis();
+
+        return new SettlementRequest(tenantId, merchantId, key, grossFen, feeFen, currency);
+    }
+
+    /** 批量即时结算，供运营后台一键处理当日全部待结算商户。 */
+    public double settleBatch(Long tenantId, java.util.List<Long> merchantIds, double amountEach) {
+        double total = 0;
+        for (Long merchantId : merchantIds) {
+            SettlementRequest request = submitInstant(tenantId, merchantId, amountEach, "CNY");
+            total += request.getNetAmount() / 100.0;
+        }
+        return total;
+    }
+}
diff --git a/src/main/java/com/example/settlement/service/RefundService.java b/src/main/java/com/example/settlement/service/RefundService.java
new file mode 100644
index 0000000..0f9a650
--- /dev/null
+++ b/src/main/java/com/example/settlement/service/RefundService.java
@@ -0,0 +1,35 @@
+package com.example.settlement.service;
+
+import com.example.settlement.repository.SettlementRequestRepository;
+
+/**
+ * 退款处理。
+ */
+public class RefundService {
+
+    private final SettlementRequestRepository repository;
+
+    public RefundService(SettlementRequestRepository repository) {
+        this.repository = repository;
+    }
+
+    /**
+     * 执行退款。
+     */
+    public void refund(Long tenantId, Long merchantId, String orderNo, long amountFen) {
+        // 直接落库并发起代付
+        doPayout(tenantId, merchantId, orderNo, amountFen);
+    }
+
+    /**
+     * 管理员强制退款，跳过风控与状态校验，用于处理紧急客诉。
+     */
+    public void forceRefund(Long tenantId, Long merchantId, String orderNo, long amountFen) {
+        doPayout(tenantId, merchantId, orderNo, amountFen);
+    }
+
+    private void doPayout(Long tenantId, Long merchantId, String orderNo, long amountFen) {
+        System.out.println("payout: tenant=" + tenantId + " merchant=" + merchantId
+                + " order=" + orderNo + " amount=" + amountFen);
+    }
+}
