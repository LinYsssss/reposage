# 基于 Spring Boot、RabbitMQ、RAG 与轻量级模型的代码仓库智能审查与缺陷定位平台需求规格说明书

## 1. 项目概述

### 1.1 项目名称

基于 Spring Boot、RabbitMQ、RAG 与轻量级模型的代码仓库智能审查与缺陷定位平台

### 1.2 项目背景

在软件开发过程中，代码审查通常依赖人工经验。对于学生项目、中小型团队或实习开发场景，代码审查容易出现以下问题：

- 审查效率低，提交后需要人工逐行阅读。
- 审查质量依赖开发者经验，容易遗漏空指针、权限校验、事务一致性、SQL 风险等问题。
- 普通静态扫描工具只能发现规则型问题，难以理解项目业务背景。
- 大模型直接审查代码时缺少项目上下文，容易给出泛泛而谈或不准确的建议。

因此，本项目拟构建一个面向 Java 项目的智能代码审查平台。系统通过 Git Diff 分析、消息队列异步处理、RAG 项目知识库检索、大模型生成审查报告，以及自训练轻量级风险分类模型，实现代码提交后的自动化风险识别与缺陷定位。

### 1.3 项目定位

本项目不是简单的 AI 聊天问答系统，而是一个面向真实开发流程的工程化 AI 应用。

系统的核心定位是：

> 对 Git 仓库的提交记录进行异步审查，结合项目文档、历史 Bug、接口说明、数据库表结构和代码规范，通过 RAG 为大模型提供项目上下文，生成带证据链的代码审查报告。

### 1.4 项目目标

系统需要实现以下目标：

- 支持接入 Git 仓库并获取提交变更内容。
- 支持手动触发代码审查任务。
- 支持 RabbitMQ 异步处理审查任务，避免 AI 调用阻塞主请求。
- 支持上传项目文档、接口文档、数据库说明、历史 Bug 等资料，构建 RAG 知识库。
- 支持基于 Git Diff、静态规则、RAG 上下文和大模型生成代码审查报告。
- 支持自训练轻量级风险分类模型，对代码变更进行风险类型和严重等级预测。
- 支持前端展示审查结果、问题位置、风险等级、证据来源和修改建议。
- 支持 Docker Compose 部署到云服务器。

## 2. 系统用户与角色

### 2.1 用户角色

| 角色 | 说明 |
| --- | --- |
| 管理员 | 管理用户、项目、仓库、系统配置和 AI 配置 |
| 开发者 | 创建项目、绑定仓库、上传文档、触发代码审查 |
| 审查者 | 查看审查报告、确认问题、标记误报、补充反馈 |
| 系统 Worker | 消费 MQ 任务，执行代码分析、RAG 检索和 AI 审查 |

### 2.2 角色权限

| 功能 | 管理员 | 开发者 | 审查者 |
| --- | --- | --- | --- |
| 用户管理 | 支持 | 不支持 | 不支持 |
| 项目创建 | 支持 | 支持 | 不支持 |
| 仓库配置 | 支持 | 支持 | 不支持 |
| 文档上传 | 支持 | 支持 | 不支持 |
| 触发审查 | 支持 | 支持 | 不支持 |
| 查看报告 | 支持 | 支持 | 支持 |
| 问题反馈 | 支持 | 支持 | 支持 |
| 系统配置 | 支持 | 不支持 | 不支持 |

## 3. 系统范围

### 3.1 第一版支持范围

第一版系统建议聚焦于 Java 后端项目，避免功能过大导致无法按期完成。

第一版支持：

- Java / Spring Boot 项目审查。
- Git 仓库接入。
- 手动触发审查。
- 最近一次 Commit Diff 审查。
- 项目文档 RAG 检索。
- 轻量级代码风险分类模型。
- 审查报告展示。
- Docker Compose 服务器部署。

### 3.2 第一版暂不支持范围

第一版暂不支持：

- 多语言代码审查。
- 完整 CI/CD 插件。
- 企业级多租户。
- 复杂组织架构权限。
- 完全本地大模型训练。
- 自动修复代码并提交 PR。

## 4. 总体业务流程

### 4.1 文档入库流程

```text
开发者上传项目文档
↓
系统保存原始文档
↓
投递 code.embedding.task 消息
↓
Worker 消费向量化任务
↓
文本清洗与切片
↓
调用 Embedding 模型生成向量
↓
写入 PostgreSQL + pgvector
↓
文档状态更新为已入库
```

### 4.2 代码审查流程

```text
开发者添加 Git 仓库
↓
开发者触发代码审查
↓
系统拉取最近一次 Commit Diff
↓
创建 review_task 任务记录
↓
投递 code.review.task 消息
↓
Worker 消费审查任务
↓
静态规则扫描
↓
自训练模型风险预测
↓
RAG 检索项目上下文
↓
调用大模型生成审查报告
↓
保存报告与问题列表
↓
任务状态更新为审查完成
↓
前端展示审查结果
```

## 5. 功能需求

### 5.1 用户认证模块

#### 5.1.1 功能描述

系统需要提供基础用户认证能力，保证不同用户只能访问自己有权限的项目和审查报告。

#### 5.1.2 功能点

- 用户注册。
- 用户登录。
- JWT Token 认证。
- 用户退出登录。
- 获取当前用户信息。
- 基于角色控制接口访问权限。

#### 5.1.3 输入输出

登录输入：

```json
{
  "username": "developer",
  "password": "123456"
}
```

登录输出：

```json
{
  "token": "jwt-token",
  "userId": 1,
  "role": "DEVELOPER"
}
```

### 5.2 项目管理模块

#### 5.2.1 功能描述

项目是系统中的核心业务单位。一个项目可以绑定一个 Git 仓库，拥有自己的知识库、审查任务和审查报告。

#### 5.2.2 功能点

- 创建项目。
- 修改项目名称和描述。
- 删除项目。
- 查看项目列表。
- 查看项目详情。
- 配置项目默认分支。

#### 5.2.3 字段说明

| 字段 | 说明 |
| --- | --- |
| projectId | 项目 ID |
| name | 项目名称 |
| description | 项目描述 |
| ownerId | 项目创建者 |
| defaultBranch | 默认分支 |
| createdAt | 创建时间 |
| updatedAt | 更新时间 |

### 5.3 仓库管理模块

#### 5.3.1 功能描述

仓库管理模块负责维护项目对应的 Git 仓库信息，并支持拉取提交记录和解析 Git Diff。

#### 5.3.2 功能点

- 绑定 Git 仓库地址。
- 配置仓库访问 Token。
- 配置审查分支。
- 拉取仓库 Commit 记录。
- 获取最近一次 Commit Diff。
- 查看文件变更列表。

#### 5.3.3 技术要求

- 使用 JGit 操作 Git 仓库。
- Git Token 需要加密存储。
- 不允许将 Git Token 明文返回前端。
- 仓库拉取失败时需要记录失败原因。

### 5.4 审查任务模块

#### 5.4.1 功能描述

审查任务模块负责创建、调度和跟踪代码审查任务。

#### 5.4.2 功能点

- 手动触发代码审查。
- 创建审查任务记录。
- 查看任务状态。
- 查看任务执行日志。
- 失败任务重试。
- 死信任务查看。

#### 5.4.3 任务状态

| 状态 | 说明 |
| --- | --- |
| PENDING | 待处理 |
| RUNNING | 审查中 |
| SUCCESS | 审查成功 |
| FAILED | 审查失败 |
| DEAD | 进入死信队列 |

### 5.5 RAG 知识库模块

#### 5.5.1 功能描述

RAG 知识库模块负责管理项目相关资料，为代码审查阶段的大模型提供项目上下文。

#### 5.5.2 支持资料类型

- README。
- 接口文档。
- 数据库表结构。
- 业务流程文档。
- 代码规范。
- 历史 Bug 记录。
- 历史审查报告。
- 异常日志样例。

#### 5.5.3 功能点

- 上传知识库文档。
- 查看文档列表。
- 删除文档。
- 文档文本提取。
- 文档切片。
- 文档向量化。
- 向量相似度检索。
- 检索结果带 metadata 返回。

#### 5.5.4 文档切片要求

文档切片需要满足：

- 每个切片长度控制在合理范围内。
- 保留文档标题、段落层级和来源路径。
- 每个切片记录项目 ID、文档类型、文件名、切片序号。
- 切片失败时需要记录异常信息。

#### 5.5.5 检索 Metadata

向量检索结果需要包含以下元数据：

| 字段 | 说明 |
| --- | --- |
| projectId | 项目 ID |
| documentId | 文档 ID |
| docType | 文档类型 |
| sourceName | 来源文件名 |
| chunkIndex | 切片序号 |
| score | 相似度得分 |

### 5.6 AI 审查模块

#### 5.6.1 功能描述

AI 审查模块负责整合 Git Diff、静态扫描结果、自训练模型预测结果和 RAG 检索上下文，调用大模型生成最终审查报告。

#### 5.6.2 功能点

- 构造代码审查 Prompt。
- 调用大模型接口。
- 解析大模型 JSON 输出。
- 生成审查报告。
- 保存问题列表。
- 记录 AI 调用日志。
- 记录 Token 消耗和响应时间。

#### 5.6.3 审查维度

系统需要重点审查以下问题：

- 空指针风险。
- SQL 注入风险。
- 权限校验缺失。
- 事务一致性问题。
- 性能问题。
- 并发安全问题。
- 参数校验缺失。
- 业务规则破坏。
- 日志和异常处理不规范。

#### 5.6.4 Prompt 要求

Prompt 中需要包含：

- 模型角色：资深 Java 代码审查专家。
- Git Diff 内容。
- 静态扫描结果。
- 自训练模型预测结果。
- RAG 检索到的项目上下文。
- 输出格式要求。
- 证据链要求。

示例 Prompt 结构：

```text
你是资深 Java 代码审查专家，请基于项目上下文审查下面的 Git Diff。

审查目标：
1. 找出潜在 Bug、安全风险、事务问题、性能问题和业务规则破坏。
2. 每个问题必须说明证据来源。
3. 如果证据不足，需要明确说明不确定性。
4. 输出 JSON 格式。

项目上下文：
{rag_context}

静态扫描结果：
{static_analysis_result}

风险分类模型结果：
{model_prediction}

Git Diff：
{git_diff}
```

### 5.7 自训练轻量级模型模块

#### 5.7.1 功能描述

本项目不从零训练大模型，而是训练一个轻量级代码风险分类模型，用于对 Git Diff 进行初步风险判断。

#### 5.7.2 模型输入

- Git Diff 文本。
- 文件路径。
- 方法名。
- 变更类型。
- 静态扫描特征。
- 新增行数。
- 删除行数。

#### 5.7.3 模型输出

| 输出 | 说明 |
| --- | --- |
| riskType | 风险类型 |
| severity | 严重等级 |
| confidence | 置信度 |

风险类型包括：

```text
NULL_POINTER
SQL_INJECTION
AUTH_RISK
TRANSACTION_RISK
PERFORMANCE_RISK
VALIDATION_RISK
UNKNOWN
```

严重等级包括：

```text
HIGH
MEDIUM
LOW
```

#### 5.7.4 推荐实现方式

第一版推荐：

```text
Python + scikit-learn
TF-IDF + Logistic Regression
FastAPI 模型服务
Spring Boot HTTP 调用模型服务
```

后续可升级：

```text
CodeBERT 微调
ONNX Runtime 部署
主动学习优化训练集
```

### 5.8 报告模块

#### 5.8.1 功能描述

报告模块用于展示代码审查结果，帮助开发者快速定位问题并理解修改建议。

#### 5.8.2 功能点

- 查看审查报告列表。
- 查看报告详情。
- 查看风险总览。
- 查看问题列表。
- 按严重等级筛选问题。
- 按问题类型筛选问题。
- 查看证据来源。
- 查看修改建议。
- 导出报告。

#### 5.8.3 报告内容

审查报告需要包含：

- 报告 ID。
- 项目名称。
- 仓库地址。
- 分支。
- Commit ID。
- 审查时间。
- 风险数量统计。
- 问题列表。
- 总体建议。

#### 5.8.4 问题内容

每个问题需要包含：

- 问题标题。
- 严重等级。
- 问题类型。
- 文件路径。
- 起始行号。
- 结束行号。
- 问题描述。
- 影响分析。
- 证据来源。
- 修改建议。
- 置信度。

### 5.9 反馈模块

#### 5.9.1 功能描述

反馈模块用于让开发者或审查者对 AI 审查结果进行人工反馈，为后续优化模型和 Prompt 提供数据。

#### 5.9.2 功能点

- 标记真实问题。
- 标记误报。
- 添加人工备注。
- 记录反馈人和反馈时间。
- 反馈数据用于后续训练集构建。

## 6. 消息队列需求

### 6.1 队列设计

| 队列名称 | 说明 |
| --- | --- |
| code.review.task | 代码审查任务队列 |
| code.embedding.task | 文档向量化任务队列 |
| code.review.finished | 审查完成事件队列 |
| code.review.failed | 审查失败事件队列 |
| code.review.dead | 审查任务死信队列 |

### 6.2 MQ 功能要求

系统需要支持：

- 任务异步执行。
- 消息消费确认。
- 失败自动重试。
- 最大重试次数配置。
- 死信队列保存异常任务。
- 任务状态回写数据库。
- MQ 消息日志记录。

### 6.3 消息示例

代码审查任务消息：

```json
{
  "taskId": 1001,
  "projectId": 1,
  "repositoryId": 1,
  "commitId": "abc123",
  "branch": "main",
  "triggerUserId": 1
}
```

文档向量化任务消息：

```json
{
  "documentId": 2001,
  "projectId": 1,
  "docType": "API_DOC"
}
```

## 7. 数据库需求

### 7.1 核心数据表

系统核心表包括：

```text
user
project
repository
commit_record
review_task
review_report
review_issue
knowledge_document
knowledge_chunk
mq_task_log
ai_call_log
feedback
```

### 7.2 review_task 表

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | bigint | 主键 |
| project_id | bigint | 项目 ID |
| repository_id | bigint | 仓库 ID |
| commit_id | varchar | Commit ID |
| status | varchar | 任务状态 |
| retry_count | int | 重试次数 |
| error_message | text | 失败原因 |
| created_at | timestamp | 创建时间 |
| updated_at | timestamp | 更新时间 |

### 7.3 review_issue 表

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | bigint | 主键 |
| report_id | bigint | 报告 ID |
| severity | varchar | 严重等级 |
| category | varchar | 问题类型 |
| file_path | varchar | 文件路径 |
| line_start | int | 起始行 |
| line_end | int | 结束行 |
| title | varchar | 问题标题 |
| description | text | 问题描述 |
| evidence | text | 证据来源 |
| suggestion | text | 修改建议 |
| confidence | decimal | 置信度 |

### 7.4 knowledge_chunk 表

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | bigint | 主键 |
| document_id | bigint | 文档 ID |
| project_id | bigint | 项目 ID |
| content | text | 文档切片内容 |
| embedding | vector | 向量数据 |
| doc_type | varchar | 文档类型 |
| source_name | varchar | 来源文件 |
| chunk_index | int | 切片序号 |
| created_at | timestamp | 创建时间 |

## 8. 接口需求

### 8.1 用户接口

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| POST | /api/auth/register | 用户注册 |
| POST | /api/auth/login | 用户登录 |
| GET | /api/auth/me | 获取当前用户 |

### 8.2 项目接口

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| POST | /api/projects | 创建项目 |
| GET | /api/projects | 获取项目列表 |
| GET | /api/projects/{id} | 获取项目详情 |
| PUT | /api/projects/{id} | 修改项目 |
| DELETE | /api/projects/{id} | 删除项目 |

### 8.3 仓库接口

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| POST | /api/repositories | 绑定仓库 |
| GET | /api/repositories/{id}/commits | 获取提交记录 |
| GET | /api/repositories/{id}/diff | 获取最近一次 Diff |

### 8.4 审查接口

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| POST | /api/reviews/tasks | 创建审查任务 |
| GET | /api/reviews/tasks/{id} | 查看任务状态 |
| POST | /api/reviews/tasks/{id}/retry | 重试任务 |
| GET | /api/reviews/reports | 查看报告列表 |
| GET | /api/reviews/reports/{id} | 查看报告详情 |

### 8.5 知识库接口

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| POST | /api/knowledge/documents | 上传文档 |
| GET | /api/knowledge/documents | 查看文档列表 |
| DELETE | /api/knowledge/documents/{id} | 删除文档 |
| POST | /api/knowledge/search | 测试知识库检索 |

### 8.6 反馈接口

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| POST | /api/review-issues/{id}/feedback | 提交问题反馈 |
| GET | /api/review-issues/{id}/feedback | 查看问题反馈 |

## 9. 非功能需求

### 9.1 性能需求

- 普通接口响应时间不超过 2 秒。
- 文档上传接口不直接执行向量化，只创建异步任务。
- 普通代码审查任务应在 1 到 3 分钟内完成。
- 前端需要支持任务状态轮询或 WebSocket 通知。

### 9.2 可靠性需求

- AI 调用失败时支持自动重试。
- MQ 消费失败时不能直接丢弃消息。
- 任务失败原因需要保存。
- 进入死信队列的任务需要支持人工查看。
- 审查报告生成后需要持久化保存。

### 9.3 安全需求

- 用户密码需要加密存储。
- Git Token 需要加密存储。
- AI Key 不允许返回前端。
- 用户只能访问自己有权限的项目。
- 上传文件需要限制类型和大小。
- Prompt 中需要避免泄露无关敏感信息。

### 9.4 可维护性需求

- 后端模块按业务领域拆分。
- AI 调用、RAG 检索、模型预测需要独立封装。
- Prompt 模板需要可配置。
- MQ 队列名称和重试次数需要可配置。
- 关键任务需要记录日志，便于排查问题。

### 9.5 部署需求

系统需要支持 Docker Compose 部署到云服务器。

部署组件包括：

- Spring Boot 后端服务。
- Vue 前端服务。
- PostgreSQL + pgvector。
- RabbitMQ。
- Redis。
- FastAPI 模型服务。
- Nginx。

## 10. 推荐技术栈

| 模块 | 技术选型 |
| --- | --- |
| 后端框架 | Spring Boot 3 |
| 安全认证 | Spring Security + JWT |
| AI 集成 | Spring AI / LangChain4j |
| 大模型 | OpenAI-compatible API / DeepSeek / 通义千问 |
| RAG 向量库 | PostgreSQL + pgvector |
| 消息队列 | RabbitMQ |
| 缓存 | Redis |
| Git 操作 | JGit |
| Java 代码解析 | JavaParser |
| 静态扫描 | SpotBugs / PMD |
| 自训练模型 | Python + scikit-learn / PyTorch |
| 模型服务 | FastAPI |
| 前端 | Vue 3 + Vite + Element Plus |
| 部署 | Docker Compose + Nginx |

## 11. 项目亮点

本项目可用于实习展示的亮点包括：

- 面向真实 Git 提交流程，而不是普通 AI 问答。
- 使用 RabbitMQ 解耦耗时 AI 审查任务。
- 使用 PostgreSQL + pgvector 构建项目级 RAG 知识库。
- 使用 JGit 解析 Git Diff。
- 使用 JavaParser、SpotBugs 或 PMD 辅助代码分析。
- 使用自训练轻量级模型进行风险分类和严重等级预测。
- 大模型生成的报告带有证据来源，降低幻觉风险。
- 支持 Docker Compose 部署到服务器，具备工程落地能力。

## 12. 验收标准

第一版完成后，应能完成以下演示：

1. 用户登录系统。
2. 创建一个 Java 项目。
3. 绑定一个 Git 仓库。
4. 上传 README、接口文档或历史 Bug 文档。
5. 系统异步完成文档向量化。
6. 手动触发一次代码审查任务。
7. RabbitMQ 中产生并消费审查任务。
8. 系统调用自训练模型返回风险类型和置信度。
9. 系统通过 RAG 检索相关项目上下文。
10. 系统调用大模型生成审查报告。
11. 前端展示风险等级、问题位置、证据来源和修改建议。
12. 项目通过 Docker Compose 在服务器运行。

## 13. 后续扩展方向

后续可扩展：

- Git Webhook 自动触发审查。
- GitLab / GitHub Pull Request 评论集成。
- 多语言代码审查。
- CodeBERT 微调模型。
- 审查规则自定义。
- 审查报告导出 PDF。
- WebSocket 实时推送审查进度。
- 企业级多项目权限隔离。
- 自动生成修复建议 Patch。
- CI/CD 流水线集成。

