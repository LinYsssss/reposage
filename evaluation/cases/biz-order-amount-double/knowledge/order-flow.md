# 订单业务流程（节选）

> 适用范围：acme-shop 订单服务

## 金额约定

- 订单金额字段 `amount` 单位为**分**，数据库为 `bigint` 整型，**禁止浮点类型**。
- 优惠券抵扣、运费、实付金额分别独立字段，不得合并计算后再拆分。
- **任何金额计算禁止使用浮点类型**：浮点乘除后强转会产生分级误差，历史事故 EVAL-206 因用 `double` 重算实付金额造成批量订单对账差异。
- 折扣、费率等比例值一律使用**基点（万分之一）整数**参与运算，严禁以「元」为单位的浮点中间值。
