# template: checklist-ts-v1（chat 审查·任务层清单槽：TS/JS 前端文件类型定制清单，r8-R2）
# 来源: .trellis/tasks/08-03-r8-prompt-tuning/research/r2-checklist-evidence.md §2.2 三条逐字落地——
# 条目 1-3 对应研究稿 T1-T3，背书映射见同表；holdout-only 背书条目为 2（§2.4）。PRD 称「Vue+JS」，
# 语料现实为纯 .ts（§1.3），点名的 XSS/契约字段漂移无背书不准入，进待回灌候选（§2.3）。
# 措辞不指定输出 category（§3.5）。
# 注入: 分片 diff 含 .vue/.ts/.tsx/.js/.jsx/.mjs 扩展名时命中（§3.2 frontend-ts 类型键）；
# 作为 chat-review-task-v2 清单槽的槽值注入，不经 formatted() 复扫。
# 经 layer() 加载：正文逐字节保留（末尾换行是与后续段落拼接的结构字节）。

审查重点（TS/JS）：
1. 新增资源入口的属主校验:新增"按 id 操作资源"的 endpoint/handler(尤其删除/修改等破坏性操作)是否调用与同文件既有入口一致的属主/权限断言(`assertOwnership` 形态)
2. 路径拼接:用户可控路径片段与根目录 `join` 后是否缺规范化与前缀校验(path traversal)
3. 可空解引用:可选参数(`user?: {...}`)与可空字段是否未经判定直接 `.prop` 链式访问
