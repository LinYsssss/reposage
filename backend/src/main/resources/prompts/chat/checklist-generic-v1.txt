# template: checklist-generic-v1（chat 审查·任务层清单槽：通用六大类回落清单，r8-R2）
# 来源: chat-review-task-v1 的「审查重点」六大类块逐字迁入（review-task-v1.txt 原 10-16 行）。
# 注入: 分片 diff 无 java/frontend-ts 扩展名命中（含 .py/.md/.sql/.yml/未识别路径/无文件头分片）时
# 回落本清单，保证不留空段——sql-migration/config 两类语料弹药为零不建清单文件（研究稿 §1.3/§3.2）。
# generic 回落路径的 v2 组装产出与 v1 逐字节相同（golden 测试钉死，R1 字节等价保证在该路径延续）。
# 作为 chat-review-task-v2 清单槽的槽值注入，不经 formatted() 复扫。
# 经 layer() 加载：正文逐字节保留（末尾换行是与后续段落拼接的结构字节）。

审查重点：
1. 空指针风险
2. SQL 注入风险
3. 权限校验缺失
4. 事务一致性问题
5. 性能问题
6. 业务规则破坏
